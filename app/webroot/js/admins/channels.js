$(document).ready(function() {
    /**
     * Channel Edit
     */
    $('button.edit').click(function(e) {
        e.preventDefault();
        var button_id = $(this).attr('id');
        switch (button_id) {
            case 'button_1':
                break;
            case 'button_2':
                break;
        }
    });
});